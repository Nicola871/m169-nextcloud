# Diese Datei enthält die Zugangsdaten für den mysqld_exporter.
# Sie wird vom Exporter genutzt, um sich mit der MySQL-Datenbank zu verbinden und Metriken auszulesen.

[client]
user=exporter         # Benutzername für die Verbindung zur MySQL-Datenbank
password=deinExportPasswort  # Passwort für den Benutzer
host=db               # Hostname oder Service-Name der MySQL-Datenbank (im Docker-Netzwerk meist der Service-Name)
port=3306             # Port der MySQL-Datenbank (Standard: 3306)
