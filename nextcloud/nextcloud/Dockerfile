# ./nextcloud/Dockerfile
FROM nextcloud:latest

# Beispiel: PHP-Modul oder Plugin hinzufügen (optional)
# RUN apt-get update && apt-get install -y php-imagick

# Standardmäßig keine Anpassung notwendig
